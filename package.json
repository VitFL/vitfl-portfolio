{
  "name": "vitfl-portfolio",
  "version": "1.0.0",
  "description": "VitFL front-end developer portfolio",
  "engines": {
    "node": "10.x"
  },
  "scripts": {
    "start": "node server",
    "lint": "eslint .",
    "test-all": "npm test && cd react-ui && npm test",
    "install-all": "npm i && cd react-ui && npm i",
    "build": "npm install && cd react-ui && npm install && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "react-ui/node_modules"
  ],
  "dependencies": {
    "express": "^4.17.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/VitFL/vitfl-portfolio.git"
  },
  "keywords": [
    "node",
    "heroku",
    "create-react-app",
    "react"
  ],
  "license": "MIT",
  "devDependencies": {}
}
